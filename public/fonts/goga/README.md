# Goga Test Fonts

The Goga Test font binaries are intentionally not committed to this public
repository because the source package is licensed for personal use only.

Local development can keep the `.otf` files in this directory. Git ignores those
files.

GitHub Pages deployment restores the `.otf` files from a private font repository
before running the production build.
